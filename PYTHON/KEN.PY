temp = eval(input('Enter a temperature in celsius: '))
print('In Fahrenheit, that is', 9/5*temp+32)

#input function's job is to ask the user to type something in and to capture what the user types.
#eval is used when we are getting numerical input.
